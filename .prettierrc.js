/**
 * @type {import("prettier").Config}
 */
 module.exports = {
    tabWidth: 4,
    trailingComma: "all",
    /* keep in sync with
           rules["max-len"].code
         in .eslintrc.json
    */
    printWidth: 80,
};
